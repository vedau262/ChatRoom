<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="com.festfive.app.R"/>
        <import type="com.festfive.app.extension.ContextCompatExtKt" />
        <import type="android.view.View" />

        <variable
            name="iView"
            type="com.festfive.app.view.SetupFragment" />

        <variable
            name="roomID"
            type="String" />

        <variable
            name="userName"
            type="String" />

        <variable
            name="setupDone"
            type="Boolean" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".view.chat.SocketTestActivity"
        android:layout_marginVertical="@dimen/dp_15">
        <EditText
            android:id="@+id/txtRoomId"
            android:layout_height="wrap_content"
            android:layout_width="@dimen/dp_150"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginHorizontal="@dimen/dp_32"
            style="@style/BoldText"
            android:textColor="@color/black"
            android:hint="enter room id">
        </EditText>

        <EditText
            android:id="@+id/txtUserId"
            android:layout_height="wrap_content"
            android:layout_width="@dimen/dp_150"
            android:layout_marginHorizontal="@dimen/dp_32"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/txtRoomId"
            style="@style/BoldText"
            android:textColor="@color/black"
            android:hint="enter user id"
            android:layout_marginTop="@dimen/dp_10">
        </EditText>

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_setUp"
            style="@style/MainActiveButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="set up"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            android:onClick="@{() -> iView.setupChat()}"
            android:backgroundTint="@{ContextCompatExtKt.getColor(roomID.length() >= 1 &amp;&amp; userName.length() >= 1 ? R.color.color_blue : R.color.color_grey)}"
            app:setViewDisable="@{roomID.length() &lt;= 0 ||  userName.length() &lt;= 0}" />
        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_chat"
            style="@style/MainActiveButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/color_blue"
            android:text="Chat"
            app:layout_constraintTop_toTopOf="@+id/txtUserId"
            app:layout_constraintRight_toRightOf="parent"
            android:onClick="@{() -> iView.gotoChat()}"
            android:visibility="@{setupDone? View.VISIBLE : View.INVISIBLE}"/>

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_call"
            style="@style/MainActiveButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/color_blue"
            android:text="Call"
            app:layout_constraintTop_toTopOf="@+id/txtUserId"
            app:layout_constraintRight_toLeftOf="@+id/btn_chat"
            android:layout_marginEnd="@dimen/dp_10"
            android:onClick="@{() -> iView.gotoChat()}"
            android:visibility="@{setupDone? View.VISIBLE : View.INVISIBLE}"/>

        <TextView
            android:id="@+id/txtName"
            style="@style/MainActiveButton"
            android:layout_width="@dimen/dp_150"
            android:layout_height="wrap_content"
            android:text="User Name"
            android:textColor="@color/color_red"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/txtUserId"
            android:layout_marginTop="@dimen/dp_10"/>

        <TextView
            android:id="@+id/txtId"
            style="@style/MainActiveButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Room"
            android:textColor="@color/black"
            app:layout_constraintStart_toEndOf="@+id/txtName"
            app:layout_constraintTop_toTopOf="@+id/txtName"
            android:layout_marginStart="@dimen/dp_10"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rc_user"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@+id/txtName"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="@dimen/dp_10"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>